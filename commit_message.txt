feat: integrate Alpha Vantage tool and simplify input to ticker-only

Major changes:
- Set up Alpha Vantage tool integration with schema-based response transformation
  - Tool fetches INCOME_STATEMENT and BALANCE_SHEET data from Alpha Vantage API
  - Response is automatically transformed to match financials_schema from YAML config
  - Supports configurable schema with field mappings and default values
  - Factory function create_alphavantage_tool() loads schema from config automatically

- Simplified input_data to ticker-only format
  - Removed name, industry, revenue, loan_amount, purpose fields
  - Input now only requires stock ticker symbol (e.g., "IBM", "AAPL")
  - All agents updated to use ticker for data fetching and searches
  - Tavily searches now use ticker-based queries

- Implemented Altman Z-score calculation
  - Replaced hardcoded score with actual calculation using financials data
  - Z-score = 1.2A + 1.4B + 3.3C + 0.6D + 1.0E formula implemented
  - Components calculated from Alpha Vantage financial data
  - Added logging for calculation components and final score

- Credit decision now based solely on Z-score threshold
  - Decision logic: Approve if Z-score > 2.6 and no major risks
  - Removed dependency on other input fields for decision making
  - Z-score threshold configurable in decision_rules

- Added financials_schema to YAML configuration
  - Schema defines expected financial metrics (revenue, assets, equity, debt, cash, interest_coverage_ratio)
  - Default values provided for fallback when API unavailable
  - Schema can be customized per experiment

- Enhanced credit_risk_agent to use Alpha Vantage tool
  - Automatically fetches real financial data for ticker
  - Falls back to default values on API errors
  - Integrated with financials_schema from config

- Updated config_loader to support input_data and financials_schema
  - Added get_financials_schema() helper function
  - Input_data now optional in config with ticker field

Technical improvements:
- Added comprehensive error handling and logging for Alpha Vantage API calls
- Improved field mapping between Alpha Vantage API and schema fields
- Enhanced tool factory pattern for consistent tool creation
- Better exception handling with fallback to defaults
